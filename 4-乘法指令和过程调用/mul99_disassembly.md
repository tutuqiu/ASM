mul99:  file format mach-o arm64

Disassembly of section __TEXT,__text:

0000000100000460 <_main>:
100000460: d10103ff     sub   sp, sp, #0x40          // 给 main 分配 0x40=64 字节栈空间
100000464: a9037bfd     stp   x29, x30, [sp, #0x30]  // 保存旧的 x29(帧指针)、x30(返回地址)
100000468: 9100c3fd     add   x29, sp, #0x30         // x29 指向新的栈帧顶部 (sp+0x30)

10000046c: b81fc3bf     stur  wzr, [x29, #-0x4]      // 局部变量初始化为 0（这里用不到，相当于占位）

; i = 9;
100000470: 52800128     mov   w8, #0x9               // w8 = 9
100000474: b81f83a8     stur  w8, [x29, #-0x8]       // [x29-8] 作为外层循环变量 i

; 进入外层 for 循环判断部分
100000478: 14000001     b     0x10000047c           // 跳到外层循环条件判断

; 外层循环条件：while (i > 0)
10000047c: b85f83a8     ldur  w8, [x29, #-0x8]       // 取出 i
100000480: 71000108     subs  w8, w8, #0x0           // i - 0，设置标志位
100000484: 540004ed     b.le  0x100000520            // i <= 0 时跳到 0x520，结束所有循环

; 进入外层循环体，先初始化内层变量 j
100000488: 14000001     b     0x10000048c

; j = 1;
10000048c: 52800028     mov   w8, #0x1               // w8 = 1
100000490: b81f43a8     stur  w8, [x29, #-0xc]       // [x29-0x0c] 作为内层循环变量 j

; 跳到内层循环条件判断
100000494: 14000001     b     0x100000498

; 内层循环条件：while (j <= i)
100000498: b85f43a8     ldur  w8, [x29, #-0xc]       // w8 = j
10000049c: b85f83a9     ldur  w9, [x29, #-0x8]       // w9 = i
1000004a0: 6b090108     subs  w8, w8, w9            // j - i
1000004a4: 540002ec     b.gt  0x100000500            // j > i 时跳出内层循环，打印换行

; 内层循环体：计算乘积并调用 printf 打印 "i x j = i*j"
1000004a8: 14000001     b     0x1000004ac

; prod = i * j;
1000004ac: b85f83a8     ldur  w8, [x29, #-0x8]       // w8 = i
1000004b0: b85f43a9     ldur  w9, [x29, #-0xc]       // w9 = j
1000004b4: 1b097d08     mul   w8, w8, w9            // w8 = i * j
1000004b8: b81f03a8     stur  w8, [x29, #-0x10]      // [x29-0x10] 存 prod

; 准备 printf 的三个整型参数（i、j、prod）
1000004bc: b85f83a8     ldur  w8, [x29, #-0x8]       // w8 = i
1000004c0: aa0803eb     mov   x11, x8               // x11 = i（第一个参数）
1000004c4: b85f43a8     ldur  w8, [x29, #-0xc]       // w8 = j
1000004c8: aa0803ea     mov   x10, x8               // x10 = j（第二个参数）
1000004cc: b85f03a8     ldur  w8, [x29, #-0x10]      // w8 = prod
1000004d0: 910003e9     mov   x9, sp                // x9 = 当前 sp（临时做参数区域）

; 把三个参数按顺序写到栈上（这里是 ABI 细节，编译器生成的调用序）
1000004d4: f900012b     str   x11, [x9]             // *(sp)     = i
1000004d8: f900052a     str   x10, [x9, #0x8]       // *(sp+8)   = j
1000004dc: f9000928     str   x8,  [x9, #0x10]      // *(sp+16)  = prod

; x0 = 格式字符串 " %d x %d = %-2d "
1000004e0: 90000000     adrp  x0, 0x100000000       // 取所在 4KB 页基址
1000004e4: 91152000     add   x0, x0, #0x548        // x0 = 格式串地址

; 调用 printf(i, j, prod)
1000004e8: 94000015     bl    0x10000053c           // 跳转到 printf 的跳板

; 内层循环自增 j++
1000004ec: 14000001     b     0x1000004f0
1000004f0: b85f43a8     ldur  w8, [x29, #-0xc]      // w8 = j
1000004f4: 11000508     add   w8, w8, #0x1          // j + 1
1000004f8: b81f43a8     stur  w8, [x29, #-0xc]      // 保存新的 j
1000004fc: 17ffffe7     b     0x100000498           // 回到内层循环条件处

; 内层循环结束：打印一个换行
100000500: 90000000     adrp  x0, 0x100000000       // x0 = "\n" 字符串所在页
100000504: 91156000     add   x0, x0, #0x558        // x0 = "\n" 地址
100000508: 9400000d     bl    0x10000053c           // printf("\n")

; 外层循环自减 i--
10000050c: 14000001     b     0x100000510
100000510: b85f83a8     ldur  w8, [x29, #-0x8]      // w8 = i
100000514: 71000508     subs  w8, w8, #0x1          // i - 1
100000518: b81f83a8     stur  w8, [x29, #-0x8]      // 更新 i
10000051c: 17ffffd8     b     0x10000047c           // 回到外层循环条件

; 当 i <= 0 时：再打印一次换行（或结束提示），然后 return 0;
100000520: 90000000     adrp  x0, 0x100000000       // x0 = "\n" 字符串所在页
100000524: 91156000     add   x0, x0, #0x558        // x0 = "\n"
100000528: 94000005     bl    0x10000053c           // printf("\n")
10000052c: 52800000     mov   w0, #0x0              // 返回值设置为 0

; 恢复栈帧并返回
100000530: a9437bfd     ldp   x29, x30, [sp, #0x30] // 恢复旧的 x29/x30
100000534: 910103ff     add   sp, sp, #0x40         // 释放 64 字节栈空间
100000538: d65f03c0     ret                         // 从 main 返回

Disassembly of section __TEXT,__stubs:

000000010000053c <__stubs>:
10000053c: 90000030     adrp  x16, 0x100004000      // 取 GOT 所在页地址
100000540: f9400210     ldr   x16, [x16]           // 从 GOT 里取出 printf 实际地址
100000544: d61f0200     br    x16                  // 跳转到真正的 printf
